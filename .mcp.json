{
  "mcpServers": {
    "postgres": {
      "command": "bash",
      "args": [
        "-c",
        "export PATH=\"/opt/homebrew/bin:$PATH\"; set -a; [ -f /Users/omstachu/code/compliance/.env ] && . /Users/omstachu/code/compliance/.env; set +a; exec uvx --python 3.12 postgres-mcp --access-mode=restricted \"${DATABASE_URL/postgresql+psycopg2:/postgresql:}\""
      ]
    }
  }
}
